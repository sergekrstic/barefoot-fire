import twColors from 'tailwindcss/colors'

export const chartColors = {
  primaryLight: twColors.violet[600],
  primary: twColors.violet[700],
  secondary: twColors.amber[400],
  secondaryDark: twColors.amber[500],
}

// Todo: integrate the versioning system, but first I need to decide if dates should be stored in a separate map
export const DATA_FORMAT_VERSION = '0.1.0'
