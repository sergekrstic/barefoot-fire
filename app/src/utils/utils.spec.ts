import { describe, expect, it } from 'vitest'

// import { convertScenarioBudgetsToPlotData } from './utils.methods'

describe('@convertScenarioBudgetsToPlotData()', () => {
  it('calculates budget events when period contains budget dates', () => {
    // const budgetEvents = convertScenarioBudgetsToPlotData(budget, overlapOneYearPeriod)
    expect(true).toBe(false)
  })
})
