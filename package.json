{
  "name": "barefoot-fire",
  "version": "0.0.4",
  "description": "A financial dashboard using PocketSmith for Obsidian",
  "keywords": [],
  "author": "Serge Krstic",
  "license": "MIT",
  "main": "main.js",
  "scripts": {
    "dev": "cd plugin/ && node esbuild.config.mjs",
    "format": "pnpm run --recursive format",
    "lint": "pnpm run --recursive lint",
    "test": "pnpm run --recursive test:ci",
    "build": "pnpm run --recursive build",
    "bump-version": "node version-bump.mjs && git add package.json manifest.json versions.json",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "prettier": "^3.3.3"
  }
}
